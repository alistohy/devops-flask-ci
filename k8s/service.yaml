apiVersion: v1
kind: Service
metadata:
  name: flask-ci-service
spec:
  selector:
    app: flask-ci
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
  type: NodePort
